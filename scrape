#!/bin/bash
# Executable script for hearing scraping only

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "Running judiciary hearing scraper..."
cd "$SCRIPT_DIR"

python3 scrape_judiciary.py
